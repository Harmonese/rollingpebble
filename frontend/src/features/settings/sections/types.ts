export type Profile = "auto" | "cpu" | "cu124";

export type SettingsSectionId = "workspace" | "lyrics" | "autoTiming" | "publishing" | "storage" | "reset";

export const settingsSectionIds: SettingsSectionId[] = [
    "workspace",
    "lyrics",
    "autoTiming",
    "publishing",
    "storage",
    "reset",
];
